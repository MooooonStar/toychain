package main

// import "encoding/hex"

// func Transfer(to string, amount int, bc *BlockChain, pub, priv string) (*Transaction, error) {
// 	publicKey, err := hex.DecodeString(pub)
// 	if err != nil {
// 		return nil, err
// 	}
// 	privateKey, err := hex.DecodeString(priv)
// 	if err != nil {
// 		return nil, err
// 	}
// 	key := &KeyPair{publicKey, privateKey}
// 	tx, err := NewTransaction()
// }
